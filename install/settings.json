{
  "_comment": "DB Analyst Framework — hook registration for ~/.claude/settings.json",
  "_instructions": [
    "Merge the 'hooks' block below into your existing ~/.claude/settings.json.",
    "If you already have hooks configured, add these entries to the existing arrays.",
    "Adjust the python path if needed (use 'python3' on some systems)."
  ],

  "hooks": {
    "PreToolUse": [
      {
        "_comment": "Safety hook — blocks DDL/DML SQL before execution",
        "matcher": "Bash|mcp__.*",
        "hooks": [
          {
            "type": "command",
            "command": "python ~/.claude/hooks/db-safety.py"
          }
        ]
      },
      {
        "_comment": "Cost gate — prompts for confirmation before expensive queries on Oracle/Athena",
        "matcher": "Bash|mcp__.*",
        "hooks": [
          {
            "type": "command",
            "command": "python ~/.claude/hooks/db-cost-gate.py"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "_comment": "Doc prompt — offers to save /db-explain or /db-orient results to the KB",
        "matcher": "Bash|mcp__.*",
        "hooks": [
          {
            "type": "command",
            "command": "python ~/.claude/hooks/db-doc-prompt.py"
          }
        ]
      },
      {
        "_comment": "Capture prompt — offers to save significant query results to the KB",
        "matcher": "Bash|mcp__.*",
        "hooks": [
          {
            "type": "command",
            "command": "python ~/.claude/hooks/db-capture-prompt.py"
          }
        ]
      }
    ]
  }
}
